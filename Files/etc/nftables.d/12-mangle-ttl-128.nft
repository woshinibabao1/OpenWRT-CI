## 统一 WAN 侧出方向 TTL / Hop Limit —— 防「多设备共享上网」检测
##
## 原理：不同操作系统的初始 TTL 不同（Windows 128、Linux/Android 64、macOS 64），
## 逐跳递减后在出口侧会同时出现 63/64/127/128 等多种值。DPI 据此判定
## 「同一出口后面挂了多台设备」。在 postrouting 统一改写成一个定值，
## 所有出包看起来都来自同一台设备。
##
## 参考：《OpenWrt 预防校园网多设备检测配置》
##   https://www.cnblogs.com/z-addone/p/19855795
##   （原方案针对校园网锐捷/深信服 DPI；5G CPE 共享上网是同一类检测，同样适用）
##
## ── 为什么用 $wan_devices 而不是写死接口名 ────────────────────────────
## 原方案写的是 oifname "wan" / "pppoe-wan"，那是 x86 软路由的习惯命名。
## 本机（H5000M）实测 `fw4 print`：
##     define wan_devices = { "eth1", "eth2" }
##   eth1 = 有线 WAN 口，eth2 = 5G 模组（USB CDC-NCM）
## 写死 "wan" 在这里一条都匹配不上，规则会静默失效。
## $wan_devices 由 fw4 按防火墙 zone 生成，换接口、换固件都不会失效。
##
## ── 为什么这个文件能引用到 $wan_devices ──────────────────────────────
## /etc/nftables.d/README：本目录下所有 *.nft 由 fw4 在 `inet fw4` 表内 include，
## 而 include 指令（实测位于 define 之后）与 define 处于同一解析单元，宏可见。
## 已在真机用 `fw4 print | nft -c -f -` 校验通过（RC=0）。
##
## ── priority 300 的含义 ──────────────────────────────────────────────
## 必须在 postrouting（路由决策之后），否则取不到真实出接口 oifname。
## 300 大于 fw4 的 srcnat（nat 类型，priority 100），保证在 SNAT 之后再改 TTL，
## 不会干扰 nat 链里的接口判定。
##
## ── 两条已实测确认的前提（2026-09-21 真机，H5000M / kernel 6.18.52）──────
## 1. **必须关闭 flow offload，否则本规则完全不生效。**
##    软件卸载把连接从 nftables 路径上摘走（与 UA2F 必须关卸载同理）。实测：
##      · 卸载开：客户端 HTTP 25s 超时；conntrack 条目带 [OFFLOAD]；
##        正向 10 包 / 951B，反向只回收 1 包 / 52B（仅 SYN-ACK）；
##        postrouting 计数器 packets 0 —— 本规则一个包都没处理。
##      · 卸载关：同一请求 HTTP 200 / 1.02s / 2381B，连接进出各 13 包、无 OFFLOAD 标记。
##    故 CI 侧 flow-offload 默认值已由 auto 改为 off（见 Scripts/ApplyFlowOffload.sh）。
## 2. **5G 模组（eth2）不会把 TTL 改回去。** 模组自身确实再做一层 NAT
##    （eth2 = 100.76.8.240/8，CGNAT 段），但出口抓包实测为 TTL=128：
##      TTLFIN IN=br-lan OUT=eth2 SRC=100.76.8.240 DST=111.45.11.5 ... TTL=128
##    即 SNAT 重建 IP 头后 TTL 沿用我们写入的值。此前的"需抓包确认"已结案。
##    附带发现：SNAT 重建 IP 头后 IPID 由路由器统一生成（实测 12678/12679/12680 递增），
##    所以博客提到的 IPID 指纹问题在本拓扑下天然不存在，kmod-rkp-ipid 无必要。
##
## ── 验证方法备忘（别再用错匹配条件）───────────────────────────────────
## postrouting priority > 100 时 SNAT 已完成，`ip saddr <内网IP>` 匹配不到任何包
##   （实测总计数 1325 包而客户端 0 包）—— 要用 oifname 或 `ip daddr` 匹配。
## 抓 TTL 用：`nft add rule inet fw4 <链> "oifname eth2 ip daddr <目标> log prefix X"`，
##   然后 `logread | grep X`，日志里 TTL= 字段即出口 TTL。
##
## 验证：`nft list ruleset | grep -A 5 mangle_ttl_unify`
## 回滚：删掉本文件后 `service firewall restart`

chain mangle_ttl_unify {
	type filter hook postrouting priority 300; policy accept;
	oifname $wan_devices ip ttl set 128
	oifname $wan_devices ip6 hoplimit set 128
}
